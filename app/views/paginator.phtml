<ul class="paginator clearfix">
<?php
    if($grid->current > 1)echo '<li>'.$this->tag->linkTo($this->router->getRewriteUri().($grid->before > 1 ? '?page='.$grid->before : ''), 'Назад', false).'</li>';
    if($grid->current < $grid->total_pages) echo '<li>'.$this->tag->linkTo($this->router->getRewriteUri().'?page='.$grid->next, 'Вперёд', false).'</li>';
?>
</ul>