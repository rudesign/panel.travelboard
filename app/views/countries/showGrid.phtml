<h1>Страны
    <?=
    $this->tag->linkTo($this->url->get(array(
        "for" => 'gridItemCreateForm',
        "controller" => 'countries')), '<i class="fa fa-plus-circle"></i>', false)
    ?>
</h1>
<div class="edit-grid">
    <?= $this->partial('editGridSearchForm') ?>

    <?php if(!empty($grid)): ?>
        <div class="scope">
            <?php
                $this->partial('deleteConfirmationControls');

                foreach($grid->items as $row){
                    $this->partial('countries/gridItem');
                }
            ?>
        </div>
        <?= $this->partial('paginator') ?>
    <?php endif; ?>
</div>